// Ce fichier centralise toutes les routes de l'application pour une gestion plus facile.
// Exemple : const userRoutes = require('./userRoutes');

// Exporter chaque route ici
module.exports = (app) => {
    // Exemple : app.use('/api/v1/users', userRoutes);
};
