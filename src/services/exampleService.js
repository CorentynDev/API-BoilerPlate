// Exemple de service qui contient une logique métier interne.
// Ce fichier ne fait pas d'appel externe, il se concentre sur les calculs ou traitements.

class ExampleService {
    // Une méthode qui effectue un calcul ou une logique métier simple.
    performCalculation(input) {
        // Exemple : calculer le carré d'un nombre.
        return input * input;
    }
}

module.exports = new ExampleService();
